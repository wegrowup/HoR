export class Entreprise{
  constructor(public name: string, public city: string, public activity: string,
    public website: string, public phoneNumber: string, public email: string,
  public logo: string, public adresse: string, public description: string) { }
}
